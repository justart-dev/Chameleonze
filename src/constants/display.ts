export const DISPLAY = {
  // 표시 시간 (밀리초)
  DISPLAY_TIME: 3000,

  // 애니메이션 시간 (밀리초)
  ANIMATION: {
    BACKGROUND_TRANSITION: 60000,
    FADE_IN: 300,
    FADE_OUT: 300,
    SCALE: 200,
  },
} as const;
